#include <stdio.h>
/**
 * main - function that prints all the digits from 0 to 10
 *
 *Return: (0) success
 */
int main(void)
{
	int i;

	for (i = 0; i < 10 ; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
